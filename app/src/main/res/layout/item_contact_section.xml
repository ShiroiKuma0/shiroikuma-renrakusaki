<?xml version="1.0" encoding="utf-8"?>
<!-- Letter-section header of the grouped Contacts list: an optional full-width divider on top, then the
     big section letter with an underline matching the text width (the denwa call-history header look).
     Tapping anywhere on the row folds/unfolds the section. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/section_holder"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/selector_clickable"
    android:clickable="true"
    android:focusable="true"
    android:orientation="vertical">

    <View
        android:id="@+id/section_divider"
        android:layout_width="match_parent"
        android:layout_height="4dp" />

    <!-- Vertical margins are set at bind time from the configurable section padding. -->
    <LinearLayout
        android:id="@+id/section_content"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/activity_margin"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:baselineAligned="true"
            android:orientation="horizontal">

            <org.fossify.commons.views.MyTextView
                android:id="@+id/section_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textStyle="bold"
                tools:text="A (12)" />

            <org.fossify.commons.views.MyTextView
                android:id="@+id/section_fold_indicator"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/normal_margin"
                android:textStyle="bold"
                tools:text="▸" />

        </LinearLayout>

        <View
            android:id="@+id/section_underline"
            android:layout_width="match_parent"
            android:layout_height="2dp"
            android:layout_marginTop="@dimen/tiny_margin" />

    </LinearLayout>

</LinearLayout>
